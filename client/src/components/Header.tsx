import React from 'react';

const Header: React.FC = () => {
  return (
    <header className="App-header">
      <h1 className="title">MERN-CRUD Project</h1>
    </header>
  );
};

export default Header;
